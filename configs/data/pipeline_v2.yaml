# Parámetros de construcción de data_finetune/guardrail_es_v2.parquet.
# Fuente: scripts/windows/v2_prepare_after_translate.ps1.
# Estado: registro declarativo. El código todavía no lee este archivo (ver README).
build:
  output: data_finetune/guardrail_es_v2.parquet
  max_variants_per_row: 2
  max_train_per_label: 160000
  max_eval_per_label: 12000
  max_train_per_source_label: 90000
splits:
  train: 80
  validation: 10
  test: 10
  seed: null
